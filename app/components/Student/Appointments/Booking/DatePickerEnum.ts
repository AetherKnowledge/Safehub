// I cant put it in date picker itself because it is a client component
// it wont load immideately
// so i made a separate enum file for it

export enum DatePickerSize {
  SMALL = "cally2-small",
  MEDIUM = "cally2",
}

export enum DatePickerRounded {
  ROUNDED = "rounded-box",
  SMALL_ROUNDED = "rounded",
  NONE = "",
}
